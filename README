Global setup:
 Download and install Git
  git config --global user.name "Your Name"
  git config --global user.email shabado9889@Gmail.com
        
Next steps:
  mkdir prototype
  cd prototype
  git init
  touch README
  git add README
  git commit -m 'first commit'
  git remote add origin git@github.com:jparmenter/prototype.git
  git push -u origin master
      
Existing Git Repo?
  cd existing_git_repo
  git remote add origin git@github.com:jparmenter/prototype.git
  git push -u origin master
